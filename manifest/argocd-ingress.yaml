apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: holamundo-nginx-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: 
    http:
      paths:
      - path: /helloworld
        pathType: Prefix
        backend:
          service:
            name: helloworld
            port:
              number: 80
